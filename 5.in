#x = [3, sin(x), 34];
#plot x, 2 with [lines, lines];
x = if (sin(0 AND 2), 2*x, 2);
x = 3;
x = [1, 2, 3];
push_back(x,3);
for i in range(3, sin(3))
    push_back(f, 2);
    x = 2
endfor   
for i in a+b
	x = hola + pupu;
	y = que + rico + pupu
endfor
#x = if (sin(0 AND 2), 2*x, 2);
#x = sin(0);
#x = 3;
#x = if (1 AND (0 OR 2), 2, 2)
#sin(x) = 3
plot x, 'hola' with lines;
x = 2;
x = 7
